```
#include<stdio.h>

int main(void) {
    int profit;
    double rate = 0.0;
    scanf("%d", &profit);
    int bonus;
    if (profit <= 100000) {
        rate = 0.1;
        bonus = profit * rate;
    }
    else if (profit > 100000 && profit <= 200000) {
        rate = 0.075;
        bonus = 100000 * 0.1 + (profit - 100000) * rate;
    }
    else if (profit > 200000 && profit <= 400000) {
        rate = 0.05;
        bonus = 100000 * 0.1 + 100000 * 0.075 + (profit - 200000) * rate;
    }
    else if (profit > 400000 && profit <= 600000) {
        rate = 0.03;
        bonus = 100000 * 0.1 + 100000 * 0.075 + 200000 * 0.05 + (profit - 400000) * rate;
    }
    else if (profit > 600000 && profit <= 1000000) {
        rate = 0.015;
        bonus = 100000 * 0.1 + 100000 * 0.075 + 200000 * 0.05+200000 * 0.03 + (profit - 600000) * rate;
    }
    else if (profit > 1000000) {
        rate = 0.01;
        bonus = 100000 * 0.1 + 100000 * 0.075 + 200000 * 0.05+200000 * 0.03 + 400000 * 0.015 + (profit - 1000000) * rate;
    }


    printf("%d", bonus);
    return 0;
}
```
